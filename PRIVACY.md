# Privacy

AriaLane 本身不包含分析、广告、账户系统或遥测服务，也不会把下载历史上传给
项目维护者。

应用会在本机保存界面偏好、自定义资源来源、下载历史、用户标签、智能文件夹规则、定时任务、
断线时的待发送任务和 aria2 服务器配置；RPC Secret 存放在 macOS 钥匙串中。
计划任务或待发送任务中包含的 URL、Header、Cookie 和认证信息会保存在仅当前
用户可读的应用支持目录，任务成功发送后会从待发送存档及其轮换备份中清除。
添加任务时填写的信息会按功能需要发送给你配置的 aria2 服务与下载来源。使用
远程 aria2 时，对应服务器的运营者可能看到这些网络请求与任务元数据。

AriaLane 只会在打开“添加下载”窗口或执行“从剪贴板添加”命令时读取剪贴板
文本，不会在后台持续监控剪贴板。通过 `arialane://add` 收到的链接只用于预填
添加窗口，用户确认前不会创建下载任务。

使用“网页提取”时，AriaLane 会直接访问用户输入的网页地址。默认模式只读取
HTML；启用 JavaScript 后，会在不持久保存 Cookie 与网站数据的临时 WebKit
会话中加载并执行网页脚本。对应网站仍可能看到常规网络元数据，例如 IP 地址、
请求时间和 User-Agent。

使用“资源搜索”时，AriaLane 会根据用户选择的来源，把检索词发送给 Internet
Archive、Project Gutenberg、Open Library 和/或用户自行添加的 OPDS 服务，
并读取公开目录、封面及所选馆藏的元数据，以确认公开访问状态、权利说明、访问
限制与可下载格式。选择“全部来源”会同时查询三个内置服务及所有已启用的自定义
来源。应用不会把搜索记录上传给项目维护者；这些服务仍可能按各自的隐私政策
记录常规网络元数据与检索请求。

正式 Release 启用自动更新后，Sparkle 会访问 GitHub 上的更新描述文件和安装
包。GitHub 可能按其自身隐私政策记录常规网络元数据。

下载内容、网络服务和远程 aria2 实例均由用户自行选择和控制。发布前如新增
遥测或第三方云服务，应同步更新本文件并在界面中明确说明。
